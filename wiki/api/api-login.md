# Login API (Deprecated)

Путь: `src/app/api/login/route.ts`

> ⚠️ Данный модуль закомментирован и, вероятно, не используется.
> Рекомендуется использовать `api/auth/[...all]`.

## Назначение

Ранее использовался для обработки входа пользователя (Custom implementation). В настоящее время код закомментирован.

## Интерфейс

### Входные данные

- `email` (string)
- `password` (string)

### Выходные данные

- JSON с данными пользователя или ошибкой.

## Логика работы (коротко)

Принимал email и пароль, вызывал функцию `loginUser` (также закомментированную), возвращал данные пользователя при успехе или ошибку при неудаче.

## Где используется

- Не используется (закомментирован).

## Примечания

- Функционал заменен на `better-auth`.

## Источник изменений

- Автоматическая генерация
